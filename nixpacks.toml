[phases.setup]
nixPkgs = ["...", "maven", "jdk17"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[phases.deploy]
startCommand = "java -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0 -Djava.security.egd=file:/dev/./urandom -jar target/*.jar"
